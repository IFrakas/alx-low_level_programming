#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @b: the interger to be computed
 *Return: the absolute value of thr int.
 */
int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}
